# Dev proxy target (used by Vite dev server)
VITE_API_PROXY_TARGET=http://localhost:8443

# Production API base URL (optional).
# If set, the frontend will call this origin directly instead of `/api`.
# VITE_API_BASE_URL=https://your-backend.example.com
